import { styled } from "@mui/material/styles";
import IconButton, {IconButtonProps} from '@mui/material/IconButton';

export const DigitalOfficeIconButton = styled(IconButton)<IconButtonProps>(
    ({theme}) => ({

    })
);